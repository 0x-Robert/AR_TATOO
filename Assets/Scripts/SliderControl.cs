using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class SliderControl : MonoBehaviour {
   
    public Slider slider;
    public Image tatooobj;
    void Start()
    {

       
            }
    /*
        slider = GetComponent<Slider>();
        tatooobj.rectTransform.localScale = SliderControl.value;

    }
    void Update()
    {
         slider.value = Mathf.MoveTowards(slider.value, 100.0f, 0.15f);
        //, IMAGErect.sizeDelta.x
        RectTransform rt = GetComponent(typeof(RectTransform)) as RectTransform;


    }
   
        */
}
